# c-meteo-lambda
This project is being done for recruitment purposes.

I'll mainly use java, since I have the most experience with it, knowing that in "AWS Lambda" environment it probably doesn't have the best performance (cold start).

### Open-Meteo
It's using parameter, "current=temperature_2m", as it is mentioned in documentation to return only current weather information, temperature_2m means "Air
temperature at 2 meters above ground"
